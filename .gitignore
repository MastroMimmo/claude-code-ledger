node_modules/
coverage/
*.log

# Build output: ship only the bundled single-file CLI so the plugin works
# without an install/build step. Ignore everything else under dist/.
dist/*
!dist/cli.js

# Ledger local capture data (per-repo store)
.ledger/

# Local tooling / editor state
.claude/
.fablize/

# Temporary image-generation metadata
assets/_*.json

# Windows/WSL artifacts
*:Zone.Identifier
